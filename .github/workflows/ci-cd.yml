name: CI/CD - Helm Microservices

# Trigger on pull requests to main OR manual run
on:
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allows manual run

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.set-tag.outputs.tag }}
      owner: ${{ steps.set-owner.outputs.owner }}

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set Docker image tag (first 8 chars of commit SHA)
      - name: Set image tag
        id: set-tag
        run: echo "tag=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      # 3️⃣ Set lowercase OWNER for GHCR
      - name: Set lowercase OWNER
        id: set-owner
        run: |
          echo "OWNER=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_ENV
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> $GITHUB_OUTPUT

      # 4️⃣ Logout any cached GHCR login
      - name: Logout GHCR (if cached)
        run: docker logout ghcr.io || true

      # 5️⃣ Login to GHCR
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 6️⃣ Build & push frontend
      - name: Build & push frontend
        uses: docker/build-push-action@v4
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.set-owner.outputs.owner }}/k8s-ms-frontend:${{ steps.set-tag.outputs.tag }}
            ghcr.io/${{ steps.set-owner.outputs.owner }}/k8s-ms-frontend:latest

      # 7️⃣ Build & push backend
      - name: Build & push backend
        uses: docker/build-push-action@v4
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.set-owner.outputs.owner }}/k8s-ms-backend:${{ steps.set-tag.outputs.tag }}
            ghcr.io/${{ steps.set-owner.outputs.owner }}/k8s-ms-backend:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    env:
      IMAGE_TAG: ${{ needs.build-and-push.outputs.image-tag }}
      REGISTRY: ghcr.io
      OWNER: ${{ needs.build-and-push.outputs.owner }}

    steps:
      # 1️⃣ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup kubectl
      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: '1.26.8'

      # 3️⃣ Decode Kubeconfig
      - name: Decode Kubeconfig
        env:
          KUBE_CONFIG_DATA: ${{ secrets.KUBE_CONFIG }}
        run: |
          echo "$KUBE_CONFIG_DATA" | base64 --decode > kubeconfig
          export KUBECONFIG=$PWD/kubeconfig
          kubectl config view

      # 4️⃣ Setup Helm
      - name: Setup Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.12.0

      # 5️⃣ Deploy Helm chart
      - name: Helm upgrade --install
        run: |
          echo "Deploying frontend: $REGISTRY/$OWNER/k8s-ms-frontend:$IMAGE_TAG"
          echo "Deploying backend:  $REGISTRY/$OWNER/k8s-ms-backend:$IMAGE_TAG"
          helm upgrade --install microservices charts/microservices \
            --set frontend.image="${REGISTRY}/${OWNER}/k8s-ms-frontend:${IMAGE_TAG}" \
            --set backend.image="${REGISTRY}/${OWNER}/k8s-ms-backend:${IMAGE_TAG}" \
            --wait --timeout 5m
